{
  "name": "agent-on-a-leash",
  "version": "0.1.0",
  "description": "An autonomous AI agent that can be fully hijacked — and still cannot move your funds. Built on the Ledger Agent Stack (Device Management Kit + Solana Signer Kit + Speculos).",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "main": "dist/agent.cjs",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --platform=node --format=cjs --conditions=import --target=node20 --outfile=dist/agent.cjs --sourcemap --log-level=error",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "esbuild src/policy.test.ts --bundle --platform=node --format=cjs --outfile=dist/policy.test.cjs --log-level=error && node --test dist/policy.test.cjs",
    "demo": "npm run build && node --no-deprecation dist/agent.cjs",
    "web": "esbuild src/web/server.ts --bundle --platform=node --format=cjs --conditions=import --target=node20 --outfile=dist/server.cjs --log-level=error && node --no-deprecation dist/server.cjs",
    "speculos:start": "speculos --model ${SPECULOS_MODEL:-nanosp} --display headless --api-port ${SPECULOS_API_PORT:-5000} --automation file:speculos/automation.json ${SPECULOS_APP:-apps/app.elf}",
    "start": "node --no-deprecation dist/agent.cjs"
  },
  "keywords": [
    "ledger",
    "solana",
    "ai-agent",
    "device-management-kit",
    "speculos",
    "security",
    "prompt-injection"
  ],
  "dependencies": {
    "@ledgerhq/device-management-kit": "^1.7.1",
    "@ledgerhq/device-signer-kit-solana": "^1.9.1",
    "@ledgerhq/device-transport-kit-speculos": "^1.2.1",
    "@ledgerhq/speculos-device-controller": "^0.3.0",
    "dotenv": "^16.4.5",
    "rxjs": "7.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "esbuild": "^0.24.2",
    "typescript": "^5.6.0"
  }
}
