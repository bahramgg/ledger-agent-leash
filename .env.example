# Copy this file to .env and fill in your own values.
# NEVER commit your real .env file or real API keys.

# --- LLM (the agent's reasoning) ---
# Your LLM API key. This is read from the environment and never committed.
# Replace the placeholder below with your own key in your local .env.
LLM_API_KEY=your-llm-api-key-here

# --- Solana ---
# Devnet ONLY. Never point this at mainnet.
SOLANA_CLUSTER=devnet
SOLANA_RPC_URL=https://api.devnet.solana.com

# --- Speculos (Ledger device emulator) ---
# The Speculos instance the signer talks to.
SPECULOS_API_URL=http://localhost:5000
