# App binaries are build artifacts. solana.elf is committed on purpose (a public
# test-app binary used with the Speculos test seed on devnet — no secrets) so
# `npm run dev:local` works out of the box. Ignore any other ELFs.
*.elf
!solana.elf
